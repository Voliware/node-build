const Build = require('./lib/build');
const Builder = require('./lib/builder');
const CssBuilder = require('./lib/cssBuilder');
const JsBuilder = require('./lib/jsBuilder');

module.exports = {
    Build,
    Builder,
    CssBuilder,
    JsBuilder
};